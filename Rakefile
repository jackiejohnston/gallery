require "rake"

desc "Run on localhost"
task :serve do
  sh "JEKYLL_ENV=testing jekyll serve --trace"
end

desc "Run on localhost"
task :liveserve do
  sh "JEKYLL_ENV=testing jekyll serve --trace --livereload"
end